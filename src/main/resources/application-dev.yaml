spring:
  jackson:
    serialization:
      indent-output: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:25433/avengers
    username: tortora.avenger
    password: tortora.avenger
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true